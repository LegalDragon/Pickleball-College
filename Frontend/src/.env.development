# Development Environment Configuration
# Used when running: npm run dev

# Use relative URL so Vite proxy can forward requests to the backend
VITE_API_URL=/api

# Alternative: Direct backend URL (bypasses proxy)
# VITE_API_URL=https://localhost:7009/api

# Remote API URL for development testing against deployed backend
# VITE_API_URL=https://casecapi.dink1.com/api
